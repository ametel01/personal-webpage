# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Require a production site URL for public metadata | P1 | S | - | DONE |
| 002 | Align the site header with the v1 navigation contract | P1 | S | - | DONE |
| 003 | Add a GitHub Actions quality gate for the Bun site | P1 | S | - | DONE |
| 004 | Add route-level regression tests for rendered pages and metadata routes | P2 | M | 002 | TODO |
| 005 | Resolve the PostCSS audit advisory without broad dependency churn | P2 | S/M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency notes

- 004 depends on 002 because route/regression tests should not freeze the currently incorrect mobile/desktop header contract.
- 003 can land before or after 001, but its build step should provide `NEXT_PUBLIC_SITE_URL` so it remains compatible with 001.
- 005 is independent. Add `bun audit` to CI only after 005 is complete, or CI will fail immediately.

## Recommended execution

1. 001 - Fix production metadata URL behavior first because it affects crawler-facing output.
2. 002 - Correct the visible navigation contract before adding broader route tests.
3. 003 - Add CI once the existing local gates are known and stable.
4. 004 - Add route-level regression tests after the header contract is corrected.
5. 005 - Resolve the dependency advisory with narrow dependency churn.

## Findings considered and rejected

- Dedicated direction plan for a notes/writing section: rejected for now because `docs/IMPLEMENTATION_PLAN.md` explicitly says not to add a Writing / Notes route unless at least one finished technical note exists.
- Dedicated direction plan for adding more project pages: rejected for now because `docs/PRD.md` says v1 should use three strong case studies only and add later projects only when there is real evidence.
